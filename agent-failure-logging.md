---
name: agent-failure-logging
description: エージェントの実装・調査・検証・回答などで、再発しうる誤った判断や行動、検証不足、手戻りが発生した場合に、その事実を失敗ログとして記録する。単なる環境要因や改善に利用できない事象には使用しない。
---

# Agent Failure Logging

エージェントの作業中に発生した問題を、将来のエージェント改善に利用できるよう記録する。

このSkillの責務は新しい失敗ログの記録だけとする。
失敗の傾向分析、InstructionやSkillの改善、改善効果の評価、失敗ログの削除は行わない。

## 発火条件

エージェントの作業中に、以下のような問題が発生した場合に使用する。

- 誤った実装や判断を行った
- 不適切な調査方針を選択した
- 必要な調査や検証を行わなかった
- 明示された要求、制約、ルールを満たさなかった
- コマンドやテストが失敗し、その原因がエージェントの判断・実装・操作にあった
- 問題に気づき、自力で修正した
- ユーザーから指摘されて修正した
- 大幅な手戻りややり直しが発生した
- 誤った前提に基づいて作業していたことが判明した

記録対象とするのは、同じ誤りが将来の作業でも再発しうる、または他のタスクに再利用できる判断ミス・調査ミス・検証不足である場合に限る。

最終的に正常終了した場合でも、途中に改善につながる問題があった場合は記録する。

## 記録しない条件

以下のような場合は、原則として記録しない。

- エージェントの判断や行動に問題がなく、単なる実行上の問題だった
- ネットワーク、外部サービス、実行環境など、エージェントの行動とは無関係な問題だった
- ユーザーが単純に要求を変更した
- 問題が発生したが、将来のエージェント改善に利用できる情報がない
- 同一タスク内で既に記録した同じ問題を繰り返しただけ

判断に迷う場合は、「この事象を後から分析することで、エージェントの今後の振る舞いを改善できそうか」を基準に判断する。

## 記録するタイミング

問題の内容と、少なくともエージェント側の関与が明確になった時点で記録する。

原因や結果が不明な場合は、解決または明確化した後に記録する。問題が未解決でも、エージェント側の問題が明確なら、`状態: 未解決` としてタスク終了前に記録する。

例えば、テスト失敗から実装上の問題を発見して修正した場合は、修正と検証の完了後に1件のログとして記録する。

ただし、ユーザーから明確な誤りを指摘された場合など、問題がその時点で明確になっている場合は、その時点で記録してよい。

## 記録方法

### 保存先

保存先は次の優先順位で決める。

1. ユーザーまたはタスクで指定された保存先
2. プロジェクト内ですでに使われている失敗ログ領域
3. 既存の領域がない場合は、プロジェクト直下の `failure-logs/`

同じ作業対象やプロジェクトでは、以後も同じ領域を使う。ログは原則として1つの判断ミス・調査ミス単位で1ファイルにする。ファイル名は `YYYY-MM-DD-短い説明.md` を推奨し、後から個別のログを特定できるようにする。

### 内容

自由記述を基本とするが、次の項目を短く含める。すべてを詳細に埋める必要はない。

```text
# YYYY-MM-DD: 短いタイトル

依頼:
実施しようとしたこと:
実際に起きたこと:
エージェント側の問題:
期待されていた結果:
修正内容・現在の状態:
発見の経緯:
```

原因や改善策を無理に分析・断定しない。確認できた事実を優先し、推測を含める場合は推測であることが分かるようにする。

ログには、トークン、パスワード、秘密鍵、個人情報などの機密情報をそのまま記録しない。必要な場合は伏せ字や要約を使う。

環境要因そのものを記録するのではなく、「その要因を認識せずに誤った判断をした」「検証不足があった」など、エージェントの振る舞いと結びつく事象を記録する。

例えば以下の程度でよい。

```text
# 2026-09-05

温度制御の実装を依頼した。

既存のDataXXXを参考にする必要があったが、
設計資料や類似実装を十分に確認せず、新しいデータ構造を作った。

テストで問題が発覚し、DataXXXを確認したところ、
既存の設計方針に合わせる必要があることが分かった。

実装を修正してテストは成功した。
```

## 重複記録

同一タスク内で同じ問題を繰り返した場合、同じ内容のログを大量に作成しない。既存のログが現在のタスクで作成したもの、または現在のタスクのログとして明確に特定できる場合は、そのログに経緯や修正内容を追記する。

過去ログを横断的に検索・分析して重複判定してはいけない。このSkillで許可する過去ログの閲覧は、現在のタスクのログを確認・追記するために必要な範囲に限る。

別のタスクで同様の問題が発生した場合は、新しい失敗ログとして記録する。

## 過去ログとライフサイクル

このSkillの実行時に、過去の失敗ログや改善ログを横断的に検索・閲覧・分析してはいけない。過去のログの分析と改善への利用は、ユーザーの指示で起動する別の改善プロセスが行う。

失敗ログは、次回の改善プロセスで利用する一時的な記録である。改善プロセスが必要な情報を改善ログへ反映し、削除対象を判断する。

失敗ログの削除は `agent-improvement` の責務とし、このSkillでは実行しない。

## 原則

- 最終的に成功したかどうかではなく、改善につながる問題が発生したかを基準にする
- 自己修正できた問題も記録対象とする
- 単なる実行エラーと、エージェントの判断・行動に起因する問題を区別する
- 環境要因そのものではなく、判断ミスや検証不足と結びつく事象を記録する
- 1つの判断ミス・調査ミスを1件のログにする
- 自由記述を基本とし、確認できた事実を優先する
- 機密情報をログに残さない
- 過去ログの分析や改善は行わない
