---
name: agent-failure-logging
description: エージェントの実装・調査・検証・回答などで、誤った判断や行動、手戻り、自己修正を含む改善可能な問題が発生した場合に、その内容を失敗ログとして記録する。単なる環境要因による問題や改善につながらない事象には使用しない。
---

# Agent Failure Logging

エージェントの作業中に発生した問題を、将来のエージェント改善に利用できるよう記録する。

このSkillの責務は失敗の記録のみとする。
失敗の分析、InstructionやSkillの改善、改善効果の評価は行わない。

## 発火条件

エージェントの作業中に、以下のような問題が発生した場合に使用する。

- 誤った実装や判断を行った
- 不適切な調査方針を選択した
- 必要な調査や検証を行わなかった
- 明示された要求、制約、ルールを満たさなかった
- コマンドやテストが失敗し、その原因がエージェントの判断・実装・操作にあった
- 問題に気づき、自力で修正した
- ユーザーから指摘されて修正した
- 大幅な手戻りややり直しが発生した
- 誤った前提に基づいて作業していたことが判明した

最終的に正常終了した場合でも、途中に改善につながる問題があった場合は記録する。

## 記録しない条件

以下のような場合は、原則として記録しない。

- エージェントの判断や行動に問題がなく、単なる実行上の問題だった
- ネットワーク、外部サービス、実行環境など、エージェントの行動とは無関係な問題だった
- ユーザーが単純に要求を変更した
- 問題が発生したが、将来のエージェント改善に利用できる情報がない
- 同一タスク内で既に記録した同じ問題を繰り返しただけ

判断に迷う場合は、「この事象を後から分析することで、エージェントの今後の振る舞いを改善できそうか」を基準に判断する。

## 記録するタイミング

問題の内容が明確になった時点で記録する。

作業途中に問題が発生しても、原因や結果がまだ不明な場合は、問題が解決または明確になった後に記録してよい。

例えば、

```text
テスト失敗
↓
原因調査
↓
実装上の問題を発見
↓
修正
↓
テスト成功
↓
失敗ログを記録
````

とする。

ただし、ユーザーから明確な誤りを指摘された場合など、問題がその時点で明確になっている場合は、その時点で記録してよい。

## 記録方法

失敗ログは自由記述とする。

固定されたスキーマ、分類、重要度、原因コード、発生件数などを要求しない。

人間が短時間で記録できることを優先する。

可能であれば以下を含める。

* 何を依頼したか
* 何をしようとしたか
* 何が起きたか
* 何が問題だったか
* 本来どうなってほしかったか
* どのように修正されたか
* 問題の発見や修正に至った経緯
* 今後の改善に役立ちそうな情報

原因や改善策を無理に分析・断定する必要はない。

例えば以下の程度でよい。

```text
# 2026-09-05

温度制御の実装を依頼した。

既存のDataXXXを参考にする必要があったが、
設計資料や類似実装を十分に確認せず、新しいデータ構造を作った。

テストで問題が発覚し、DataXXXを確認したところ、
既存の設計方針に合わせる必要があることが分かった。

実装を修正してテストは成功した。
```

## 重複記録

同一タスク内で同じ問題を繰り返した場合、同じ内容のログを大量に作成しない。

一つのログにまとめ、必要に応じて発生した経緯や修正内容を追記する。

別のタスクで同様の問題が発生した場合は、新しい失敗ログとして記録する。

## 過去のログを読まない

このSkillの実行時に、過去の失敗ログや改善ログを検索・閲覧・分析してはいけない。

このSkillは新たに発生した問題を記録するためだけに使用する。

過去のログの分析、失敗の傾向分析、InstructionやSkillの改善、改善効果の評価は、このSkillとは別のプロセスで行う。

## ログのライフサイクル

失敗ログは、次回の改善プロセスで利用するための一時的な記録とする。

改善プロセスで利用され、必要な情報が改善ログなどに反映された後は、失敗ログを削除してよい。

失敗ログを長期間蓄積し、過去のすべてのログを継続的に読み込ませることを目的としない。

## 原則

* 最終的に成功したかどうかではなく、改善につながる問題が発生したかを基準にする
* 自己修正できた問題も記録対象とする
* 単なる実行エラーと、エージェントの判断・行動に起因する問題を区別する
* 記録のために複雑なスキーマを作らない
* 自由記述を基本とする
* 同じ問題を同一タスク内で重複して記録しない
* 原因や改善策を無理に分析しない
* 過去のログを読まない
* InstructionやSkillの改善を行わない

