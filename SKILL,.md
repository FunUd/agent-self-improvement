---
name: agent-failure-logging
description: エージェントの作業中に、将来のエージェント改善に役立つ失敗や問題を記録する。エージェントが期待と異なる結果を出した際に使用する。
---

# Agent Failure Logging

エージェントの作業中に発生した失敗や問題を、将来のエージェント改善に利用できるよう記録する。

このSkillの責務は「失敗の記録」のみとする。
失敗の分析、InstructionやSkillの改善、改善効果の評価は行わない。

## 発火条件

以下のいずれかに該当した場合、このSkillを使用する。

- ユーザーからエージェントの誤りを指摘された
- エージェント自身が、自分の結果が要求や制約を満たしていないと認識した
- テスト、レビュー、検証などによってエージェントの誤りが判明した
- エージェントが予定していた作業を完了できず、その原因がエージェントの行動にある
- 同じ問題を繰り返し発生させた

## 発火しない条件

以下の場合、このSkillを使用しない。

- 作業途中の一時的なエラーで、最終的に正常に解決できた場合
- ユーザーの要求変更によって以前の結果が不要になった場合
- ユーザーの要求自体が不明確だったことが主な原因の場合
- 外部サービス、ネットワーク、実行環境など、エージェントの振る舞いとは無関係な一時的問題の場合
- 将来のエージェント改善に役立つ情報がない単発の偶発的問題の場合

判断に迷う場合は、記録することで将来のエージェント改善に役立つ可能性があるかを基準に判断する。

## 記録するタイミング

失敗が明確になった時点で記録する。

作業途中の一時的な失敗を毎回記録してはいけない。

例えば、以下は記録しない。

```text
コマンドが失敗
↓
原因を修正
↓
再実行
↓
正常終了
````

一方、以下は記録する。

```text
実装完了
↓
テストで問題発覚
↓
実装を修正
```

または、

```text
ユーザーから「要求と違う」と指摘された
↓
修正した
```

## 記録方法

失敗ログは自由記述とする。

固定されたスキーマ、分類、重要度、原因コード、発生件数などを要求しない。

人間が短時間で記録できることを優先する。

可能であれば以下を含める。

* 何を依頼したか
* 何が起きたか
* 何が問題だったか
* 本来どうなってほしかったか
* どのように修正されたか
* 将来の改善に役立ちそうな情報

原因を無理に分析したり、断定したりする必要はない。

## 重複記録

同一タスク中に同じ原因による問題が何度発生しても、同じ内容のログを大量に作成しない。

一つのログにまとめる。

ただし、別のタスクで同様の問題が発生した場合は、新しい失敗ログとして記録する。

## 過去の失敗ログについて

このSkillは過去の失敗ログを読むためには使用しない。

過去の失敗ログの分析や、Instruction / Skillの改善は別の改善プロセスが担当する。

このSkillの実行時に、過去の失敗ログを検索・分析・要約してはいけない。

## ログのライフサイクル

失敗ログは、次回の改善プロセスで利用するために一時的に保存する。

改善プロセスで利用された後は、改善に必要な情報が改善ログなどに反映されていることを前提として削除してよい。

失敗ログを長期間蓄積することを目的としない。

## 原則

* 「失敗したら必ずログ」ではなく「将来の改善に役立つ失敗をログする」
* 一時的な内部エラーを記録しない
* 同じ失敗を同一タスク内で大量に記録しない
* 自由記述を基本とする
* 原因分析や改善案を無理に記録しない
* 過去ログをこのSkillから読まない
* Instruction / Skillの改善は別のプロセスに任せる

```
