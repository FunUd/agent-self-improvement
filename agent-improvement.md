---
name: agent-improvement
description: ユーザーからエージェント改善の指示を受けた際に、蓄積された失敗ログを分析し、最も優先して改善すべき問題を1つ選定して提示する。ユーザーの承認後に改善を実施し、その背景・根拠・改善内容を改善ログに記録した上で、改善に使用した失敗ログを削除する。
---

# Agent Improvement Review

ユーザーから改善の指示を受けた場合に、蓄積された失敗ログを分析し、エージェント改善につなげる。

このSkillのトリガーはユーザーによる改善指示のみとする。

失敗ログが存在することや、一定期間が経過したことを理由に、自動的に改善を開始してはいけない。

## 1. 改善候補の分析

ユーザーから改善指示を受けたら、未処理の失敗ログを読み込む。

個々のログを単独で評価するだけでなく、複数のログに共通する問題や、過去に改善した問題の再発も確認する。

改善候補を以下の観点から総合的に評価する。

- エージェントへの影響が大きい
- 再発する可能性が高い
- 今後も同様の問題が発生する可能性がある
- 人間による修正や手戻りが大きい
- InstructionやSkillなどによって改善できる可能性がある
- 他のタスクにも適用できる
- 既存の改善で十分に対処できていない

単純な発生件数だけで優先順位を決めてはいけない。

## 2. 最優先の改善内容を提示する

分析後、最も優先して改善すべき問題を原則として1つ選定し、ユーザーに提示する。

提示には以下を含める。

- 改善対象となる問題
- その問題を選定した理由
- 根拠となった失敗ログの概要
- 提案する改善内容
- 期待する効果
- 改善対象となるInstructionやSkill

この時点では改善を実施してはいけない。

ユーザーの承認を待つ。

## 3. ユーザーの承認

ユーザーが改善を承認した場合のみ、提示した改善を実施する。

承認されなかった場合は、改善を実施しない。

ユーザーが別の改善を指定した場合は、その指示に従う。

## 4. 改善ログの記録

改善を実施したら、改善ログを長期保存する。

改善ログには少なくとも以下を記録する。

```text
# YYYY-MM-DD

## 背景

どのような問題が発生していたか。

## 改善理由

なぜこの問題を改善対象として選定したか。

## 根拠

改善の判断に使用した失敗ログを特定する。

## 改善内容

何をどのように変更したか。

## 期待する効果

改善によって何が変わることを期待するか。

## 関連ログ

この改善に使用した失敗ログ。

## 効果確認

今後、どのような問題の再発状況を確認するか。
````

改善ログは、後から過去の失敗ログを参照しなくても、なぜ改善したのか理解できる程度の情報を残す。

## 5. 関連する失敗ログの削除

改善ログの作成と改善の実施が完了した後、今回の改善の根拠として使用した失敗ログを削除する。

削除する前に、改善ログに必要な情報が十分に反映されていることを確認する。

改善に使用していない失敗ログは削除しない。

複数の失敗ログから1つの改善を行った場合は、その改善に関連するログをまとめて削除する。

```text
failure A ─┐
failure B ─┼→ improvement A
failure C ─┘
              ↓
        A / B / C を削除
```

改善ログには、削除した失敗ログを特定できる情報を残す。

## 6. 改善ログの扱い

改善ログは長期的に保持する。

過去の改善内容を上書きしてはいけない。

同じ問題に対して追加の改善を行う場合は、新しい改善ログを作成し、過去の改善ログとの関連を記録する。

改善ログは、将来の改善レビューで過去の改善内容や改善理由を確認するために使用する。

## 7. 改善後の扱い

改善を実施しただけでは、その改善が有効だったとは判断しない。

次回以降の改善レビューでは、過去の改善ログを参照し、同じ問題が再発していないか確認する。

改善効果を判断するための十分な情報がない場合は、判断を保留する。

## 原則

* 改善の開始はユーザーの指示を必要とする
* ログが存在するだけでは改善を開始しない
* 最優先の改善候補を原則1つ提示する
* ユーザーの承認前に改善を実施しない
* 発生件数だけで改善の優先順位を決めない
* 改善の根拠となった失敗ログを改善ログに記録する
* 改善ログを作成してから関連する失敗ログを削除する
* 改善に使用していない失敗ログは削除しない
* 改善ログは長期保存する
* 改善方法そのものを固定的なルールとして定義しない
